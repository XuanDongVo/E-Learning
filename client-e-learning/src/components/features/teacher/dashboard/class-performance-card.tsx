import { ArrowUpRight, BarChart3 } from "lucide-react";

const classes = [{ name: "6A", score: 81, delta: "+4.2%" }, { name: "7B", score: 74, delta: "+1.8%" }, { name: "8A", score: 68, delta: "-2.1%" }];

export function ClassPerformanceCard() {
  return <section className="rounded-[var(--radius-md)] border border-border-color bg-card-bg p-5 shadow-sm"><div className="flex items-start justify-between"><div><p className="text-body-sm font-bold uppercase tracking-[0.14em] text-neutral-subtle">Overview</p><h2 className="mt-1 text-card-title font-extrabold">Class performance</h2></div><BarChart3 className="h-5 w-5 text-primary" /></div><div className="mt-6 space-y-4">{classes.map((item) => <div key={item.name}><div className="mb-1 flex items-center justify-between text-body-sm"><span className="font-bold">Class {item.name}</span><span className={item.delta.startsWith("+") ? "font-bold text-emerald-600" : "font-bold text-red-500"}>{item.delta}</span></div><div className="h-2 overflow-hidden rounded-full bg-primary-light"><div className="h-full rounded-full bg-primary" style={{ width: `${item.score}%` }} /></div><p className="mt-1 text-right text-body-sm font-bold text-neutral-muted">{item.score}% average</p></div>)}</div><button className="mt-5 inline-flex items-center gap-1 text-body-sm font-extrabold text-primary">View reports <ArrowUpRight className="h-3.5 w-3.5" /></button></section>;
}